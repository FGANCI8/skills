# Sanitized handoff

Never include the raw user objective, full prompt, provider output, raw error,
secret, personal data, production/client identifier or private absolute path.
An approval reference is historical evidence only, never a grant, and grants no authority.

- Task ID (random opaque):
- Objective reference and category:
- Acceptance-criteria references:
- State: `DONE | PARTIAL | PLANNED | BLOCKED | NOT_RUN | UNKNOWN`
- Repository alias / branch / HEAD:
- Relative ownership and artifact references:
- Decision and evidence references:
- Sanitized command/check results:
- Deliberately not executed:
- Risk and blocker codes:
- Approval-record references (evidence only):
- Accounting scope: `CUMULATIVE_TASK_CHAIN`
- Cumulative limits:
- Usage at block start / block delta / cumulative usage:
- Task generation / loop instances / reopen count:
- Started at / absolute deadline:
- Append-only terminal-history references:
- Previous checkpoint reference and SHA-256:
- Rollback reference:
- Files to reread (relative or neutral aliases only):
- Exact next action code and validation reference:
